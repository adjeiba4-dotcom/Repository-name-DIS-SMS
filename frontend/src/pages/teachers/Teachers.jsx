import { TeacherPage } from "../../features/teachers";

/**
 * Teachers route entry.
 * Workspace composition lives under features/teachers.
 */
export default function Teachers() {
  return <TeacherPage />;
}
