import { SettingsPage } from "../../features/settings";

/**
 * Settings route entry.
 * Workspace composition lives under features/settings.
 */
export default function Settings() {
  return <SettingsPage />;
}
