import { ExaminationPage } from "../../features/examinations";

/**
 * Examinations page route entry.
 * Workspace composition lives under features/examinations.
 */
export default function Examinations() {
  return <ExaminationPage />;
}
