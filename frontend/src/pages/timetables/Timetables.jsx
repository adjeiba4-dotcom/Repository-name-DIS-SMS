import { TimetablePage } from "../../features/timetables";

/**
 * Timetable page route entry.
 * Workspace composition lives under features/timetables.
 */
export default function Timetables() {
  return <TimetablePage />;
}
