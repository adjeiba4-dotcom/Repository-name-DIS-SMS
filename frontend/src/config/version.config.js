/**
 * Application version metadata for Sprint tracking.
 */

const versionConfig = {
  appName: "DIS-SMS",
  version: "1.0.0",
  sprint: "Sprint 7.7 – Student Promotion & Graduation",
  batch: "Academic Operations",
  environment: import.meta.env.MODE || "development",
};

export default versionConfig;
